# SSH public keys authorized on the Proxmox containers (one per line).
# Installed for root by create_contianers.sh (--ssh-public-keys) and for the
# `ietf` deploy user by ansible/playbooks/bootstrap_ietf_user.yaml.
# Add the key(s) of your ansible control node / dev machine here before
# creating containers — and keep personal keys out of public forks.
